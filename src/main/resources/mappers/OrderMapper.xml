<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shaoyu.backend.mapper.CartMapper">

    <resultMap type="com.shaoyu.backend.model.ItemQuantity" id="OrderResult">
        <id property="id" column="id" />
        <result property="itemName" column="itemName" />
        <result property="quantity" column="quantity" />
        <result property="cartId" column="cartId" />
    </resultMap>

    <select id="selectItemsByFiltersTest"  resultMap="ItemResult">
        select * from items where name LIKE #{name}
    </select>


    <select id="selectAllOrders"  resultMap="OrderResult">
        select * from items where name LIKE #{name}
    </select>
    <select id="selectAllOrders"  resultMap="OrderResult">
        select * from items where name LIKE #{name}
    </select>
    <select id="selectAllOrders"  resultMap="OrderResult">
        select * from items where name LIKE #{name}
    </select>
    <select id="selectAllOrders"  resultMap="OrderResult">
        select * from items where name LIKE #{name}
    </select>
    <select id="selectAllOrders"  resultMap="OrderResult">
        select * from items where name LIKE #{name}
    </select>

</mapper>
